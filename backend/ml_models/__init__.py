"""Lightweight ML-facing modules for the MVP.

These files mirror the final project structure from the master guide. The
current implementation is deterministic and dependency-light so the project
runs locally without model downloads.
"""
